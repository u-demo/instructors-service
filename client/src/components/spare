// let stars = [];
    // for (let i = 1; i < this.props.cInfo.rating; i++){
    //   stars.push('full');
    // }
    // let base = Math.floor(this.props.cInfo.rating);
    // let fraction = this.props.cInfo.rating * 10 - base * 10;
    // if (fraction) {
    //   stars.push(fraction);
    // }
    // while (stars.length < 5) {
    //   stars.push('empty');
    // }